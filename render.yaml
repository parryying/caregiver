services:
  - type: web
    name: caregiver-app
    env: node
    plan: free
    buildCommand: cd backend && rm -rf node_modules package-lock.json && npm install --production
    startCommand: cd backend && node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production