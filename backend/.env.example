# Caregiver App Environment Configuration

# Database Configuration
# Leave DATABASE_URL empty to use SQLite (default for local development)
# Set DATABASE_URL to use PostgreSQL (recommended for production)
# 
# Example PostgreSQL connection strings:
# DATABASE_URL=postgresql://username:password@localhost:5432/caregiver_db
# DATABASE_URL=postgresql://user:pass@db-host:5432/database_name
DATABASE_URL=

# Server Configuration
NODE_ENV=development
PORT=3000

# For production deployment on Render/Heroku, these will be set automatically:
# NODE_ENV=production
# DATABASE_URL=postgresql://...  (from Render PostgreSQL add-on)